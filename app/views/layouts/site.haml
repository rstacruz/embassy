- content_for :head do
  != google_font "Oswald"

- content_for :body do
  != partial :'global/header'
  #content
    - if flash?
      #flash= flash

    - if content_for?(:heading)
      #heading
        != yield_content :heading

    != yield

  != partial :'global/footer'

!= partial :'layouts/base'
